import { Button } from "@/components/ui/button";

const App = () => {
  return (
    <main className="antialiased">
      <div>
        <Button variant={"outline"}>Bg red</Button>
      </div>
    </main>
  );
};

export default App;
